#termcapinfo xterm*|rxvt* 'ti@:te@:vb@'
startup_message off
defscrollback 10000
# defmonitor on
# termcapinfo rxvt-unicode ti@:te@ 
vbell off
activity ''
shelltitle '$ |'
hardstatus string 'screen / %h'
hardstatus on
# msgwait 10
hardstatus alwayslastline
hardstatus string '%{= kg}[ %{G}%H %{g}][%= %{= kw}%?%-Lw%?%{=b kR}(%{W}%n*%f %t%?(%u)%?%{=b kR})%{= kw}%?%+Lw%?%?%= %{g}][%{Y}%l%{g}]%{=b C}[ %d %M %c ]%{W}'

# Nifty for ii (irc) , but haven't really figured this all the way yet.
# bind f screen bash -c "screen -X split;screen -X resize +32; screen -X select 2; screen -X focus; screen -X select 3;exit"

# Use alt+arrows keys to navigate between windows in regions
bindkey ^[[1;3C focus down
bindkey ^[[1;3D focus
bindkey ^[[1;3A focus top
bindkey ^[[1;3B focus bottom

term screen-256color # match the color settings of st, which is 256color

altscreen on # prevent vim, etc, from appearing in scrollback
